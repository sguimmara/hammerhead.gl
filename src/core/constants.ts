export enum VertexBufferSlot {
    Position,
    TexCoord,
    Color,
    Index,
    Normals,
}

export enum BindGroups {
    GlobalValues,
    MaterialUniforms,
    ObjectUniforms,
    VertexBufferUniforms,
}
